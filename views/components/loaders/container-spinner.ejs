<style>
    .container {
      position: relative;
      top: -50%;
      /* min-height: 200px; Adjust the height as needed */
    }
    
    .loading-overlay {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(255, 255, 255, 0.8);
      display: flex;
      justify-content: center;
      align-items: center;
      z-index: 100;
    }
    
    .spinner {
      border: 4px solid #f3f3f3;
      border-top: 4px solid rgb(0, 20, 255);
      border-radius: 50%;
      width: 40px;
      height: 40px;
      animation: spin 2s linear infinite;
    }
    
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
    </style>
    
    <div id="spinnerContainer" class="container hidden">
      <!-- Your content goes here -->
      <div class="loading-overlay">
        <div class="spinner"></div>
      </div>
    </div>